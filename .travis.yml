language: python

python:
  - "2.7"


services:
  - postgresql

before_script:
  - psql -c 'create database test_db;' -U postgres

  
install:
  - pip install -r requirements.txt


script:
  - nosetests --with-coverage


after_success:
  - coveralls


  
